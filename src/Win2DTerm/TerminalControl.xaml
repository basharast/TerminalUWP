<UserControl
    x:Class="Win2DTerm.TerminalControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Unloaded="UserControl_Unloaded"
    IsTabStop="True"
    KeyDown="TerminalKeyDown"
    GotFocus="TerminalGotFocus"
    LostFocus="TerminalLostFocus"
    PointerWheelChanged="TerminalWheelChanged"
    PointerMoved="TerminalPointerMoved"
    PointerExited="TerminalPointerExited"
    PointerPressed="TerminalPointerPressed"
    PointerReleased="TerminalPointerReleased"
    Tapped="TerminalTapped"
    >
    <UserControl.Resources>
        <Style x:Key="CanvasStyle" TargetType="canvas:CanvasControl">
            <Setter Property="FontFamily" Value="{StaticResource Consolas}" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <canvas:CanvasControl x:Name="canvas" Draw="OnCanvasDraw" Style="{StaticResource  CanvasStyle}" Background="Black" />
    </Grid>
</UserControl>
